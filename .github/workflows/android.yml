name: Android CI/CD

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - uses: actions/checkout@v4

    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v4
      with:
        gradle-version: 9.5.1

    - name: Build APK
      run: |
        cd android
        gradle assembleRelease --stacktrace
        # Rename the APK to Barta.apk so the download link is consistent
        mv app/build/outputs/apk/release/app-release.apk app/build/outputs/apk/release/Barta.apk

    - name: Create Release
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      uses: ncipollo/release-action@v1
      with:
        artifacts: "android/app/build/outputs/apk/release/Barta.apk"
        tag: "latest"
        name: "Barta Latest Release"
        body: "Automated build from GitHub Actions. Download Barta.apk below."
        token: ${{ secrets.GITHUB_TOKEN }}
        allowUpdates: true
        removeArtifacts: true
        replacesArtifacts: true
