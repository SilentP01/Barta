package app.barta.messenger

import android.app.Application

class BartaApplication : Application() {
    override fun onCreate() {
        super.onCreate()
        // Phase 4: FirebaseApp.initializeApp(this)
    }
}
