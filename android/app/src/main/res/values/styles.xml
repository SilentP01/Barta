<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash screen theme - note: parent must be Theme.SplashScreen from the library -->
    <style name="Theme.Barta.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#0F1117</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.Barta</item>
    </style>

    <!-- Post-splash theme used by Compose (no title bar, transparent system bars) -->
    <style name="Theme.Barta" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">#0F1117</item>
        <item name="android:statusBarColor">#00000000</item>
        <item name="android:navigationBarColor">#00000000</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
